# 저장소 안에서는 LF 로 통일한다. 수집 스크립트가 CI(리눅스)에서 만든 JSON 과
# 로컬(윈도우)에서 만든 JSON 이 줄바꿈만으로 diff 나는 것을 막는다.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary

# 파서 골든 테스트용 원문 스냅샷. 사이트가 내려준 바이트 그대로 둔다 —
# 줄바꿈까지 포함해 그때 그 응답이어야 회귀 테스트로서 뜻이 있다.
fixtures/** -text
